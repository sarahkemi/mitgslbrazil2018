# Session 13
## Playing with more APIs

Telegram bot that adds tasks to Trello board

To use:
* use `configTemplate.py` to create a `config.py` with Trello API key & token, as well as Telegram Bot token.
* update the `LIST_ID` constant in the `bot.py` script with the Trello list ID that you want to update